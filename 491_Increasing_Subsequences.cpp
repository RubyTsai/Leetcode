class Solution {
public:
    vector<vector<int>> findSubsequences(vector<int>& nums) {
        unordered_set<int> sn;
        vector<vector<vector<int>>
    }
private:
    void helper(int i, unordered_set<int>& sn, vector<vector<vector<int>>& res) {
        if (i >= nsize) return;
        int num = nums[i];
        
        vector<int> newSeq;
        if (sn.find(num) == sn.end()) { // new number
            

        }

    }
};
