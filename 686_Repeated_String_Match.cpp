class Solution {
public:
    int repeatedStringMatch(string A, string B) {
        int sa = A.size();
        int sb = B.size();
        
        for (int i = 0; i <= sb; i++) {
            if (A[0] == B[i]) {
                        
            }
        }
    }
    
    if (sb - i >= sa) {
        if(B.find(A[a]) != std::string::npos) {
            ret++;
            if (sb - i == sa) break;
            else i += sa;
        }
    }
    else 
}; 
